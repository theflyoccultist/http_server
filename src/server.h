// This file contains the declaration of the start_server function, which is
// used to start the server.

#ifndef SERVER_H
#define SERVER_H

#include <netinet/in.h>
#include <sys/socket.h>
#include <unistd.h>

void start_server();

#endif
